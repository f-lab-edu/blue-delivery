spring:
  datasource:
    url: jdbc:mysql://localhost:3306/testdb
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    baseline-on-migrate: true
    user: root
    password: password
    url: jdbc:mysql://localhost:3306/testdb
mybatis:
  type-aliases-package: com/deliver
  mapper-locations: resources/mybatis/mapper/**/*.xml
