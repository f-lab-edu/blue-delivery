<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.delivery.shop.businesshour.BusinessHourMapper">

    <insert id="insert" parameterType="map">
        INSERT INTO BUSINESS_HOUR (SHOP_ID, OPEN, CLOSE, DAY_OF_WEEK)
        VALUES (#{shopId}, #{open}, #{close}, #{dayOfWeek})
    </insert>

    <delete id="deleteAllByShopId" parameterType="long">
        DELETE
        FROM BUSINESS_HOUR
        WHERE SHOP_ID = #{shopId}
    </delete>
</mapper>
