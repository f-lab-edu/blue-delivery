<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.delivery.restaurant.RestaurantMapper">
    <update id="updateIntroduce" parameterType="map">
        UPDATE RESTAURANTS SET INTRODUCE = #{introduce} WHERE id = #{id}
    </update>
    <update id="updatePhone" parameterType="map">
        UPDATE RESTAURANTS SET PHONE = #{phone} WHERE id = #{id}
    </update>
    <update id="updateDeliveryAreaGuide" parameterType="map">
        UPDATE RESTAURANTS SET DELIVERY_AREA_GUIDE = #{guide} WHERE id = #{id}
    </update>
</mapper>